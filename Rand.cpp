#include<bits/stdc++.h>

#define set(a,b) memset(a,(b),sizeof(a))

using namespace std;

typedef long long ll;

void rand_data()
{
  freopen(".in","w",stdout);
  srand(time(NULL));
}

int main()
{
  ios::sync_with_stdio(false);
  rand_data();
  return 0;
}
